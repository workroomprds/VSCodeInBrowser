<!DOCTYPE html>
<html>
<head>
    <title>For {{ group_name }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 40px;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
        }
        .user-info {
            margin: 20px 0;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>For {{ group_name }}</h1>
        {% for user in users %}
        <div class="user-info">
            <h2>{{ user.username }}</h2>
            <p>Access your environment at: <a href="/{{ user.username }}/">/{{ user.username }}/</a></p>
            <p>Password: {{ user.password }}</p>
        </div>
        {% endfor %}
    </div>
</body>
</html>
